--- ./Makefile.uw	2003-11-06 15:41:29.000000000 +0000
+++ ./Makefile	2003-11-07 22:19:12.930000010 +0000
@@ -11,7 +11,7 @@
 	for d in $(SUBDIRS); do ( cd $$d ; make $@ ) ; done
 
 drakconf: 
-	cp -l control-center drakconf
+	cp control-center drakconf
 
 clean:
 	$(MAKE) -C po $@
@@ -20,7 +20,9 @@
 
 install: all
 	$(MAKE) -C po $@
-	install -d $(DESTDIR)/usr/{bin/,share/{icons,doc/$(NAME)-$(VERSION)}}
+	install -d $(DESTDIR)/usr/bin/
+	install -d $(DESTDIR)/usr/share/icons
+	install -d $(DESTDIR)/usr/share/doc/packages/$(NAME)-$(VERSION)
 	install -d $(DESTDIR)/usr/sbin
 	install -d $(mcc_dir)/themes/default/
 	install -m644 control-center.rc $(mcc_dir)/themes/default/gtkrc
@@ -30,17 +32,17 @@
 	install -m755 menus_launcher.pl $(DESTDIR)/usr/sbin/
 	install -m755 print_launcher.pl $(DESTDIR)/usr/sbin/
 	install -m755 drakxconf $(DESTDIR)/usr/sbin/
-	install -m644 COPYING  $(DESTDIR)/usr/share/doc/$(NAME)-$(VERSION)/
+	install -m644 COPYING  $(DESTDIR)/usr/share/doc/packages/$(NAME)-$(VERSION)/
 	for d in $(SUBDIRS); do ( cd $$d ; make $@ ) ; done
 
 dis: clean
-	for i in pixmaps/default/mcc-left-back{,-bottom,-middle,-top}.png; do convert -flop $$i $${i%.png}_rtl.png;done 
+	for i in pixmaps/default/mcc-left-back.png pixmaps/default/mcc-left-back-bottom.png pixmaps/default/mcc-left-back-middle.png pixmaps/default/mcc-left-back-top.png; do convert -flop $$i $${i%.png}_rtl.png;done 
 
 
 
 	rm -rf $(NAME)-$(VERSION) ../$(NAME)-$(VERSION).tar*
 	mkdir -p $(NAME)-$(VERSION)
-	find . -not -name "$(NAME)-$(VERSION)" | cpio -pd $(NAME)-$(VERSION)/
+	find . ! -name "$(NAME)-$(VERSION)" | cpio -pd $(NAME)-$(VERSION)/
 	find $(NAME)-$(VERSION) -type d -name CVS -o -name .cvsignore -o -name unused -name control-center.rc -o -name controlcenter.html |xargs rm -rf
 	find $(NAME)-$(VERSION) -name '*.pl' -o -name control-center | xargs perl -pi -e 's/\s*use\s+(diagnostics|vars|strict).*//g'
 	tar cjf ../$(NAME)-$(VERSION).tar.bz2 $(NAME)-$(VERSION)
@@ -65,4 +67,4 @@
 	cvs commit -m "Generated by cvs2cl the `date '+%d_%b'`" ChangeLog
 
 
-log: changelog
\ No newline at end of file
+log: changelog
